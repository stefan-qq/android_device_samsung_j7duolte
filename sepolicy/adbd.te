# Android 7.1 adbd uses the kernel legacy transport when FunctionFS ep0 is
# absent at daemon startup. The base Nougat policy already carries this rule;
# keep it explicit in the device policy because this recovery depends on it.
allow adbd adb_device:chr_file rw_file_perms;
